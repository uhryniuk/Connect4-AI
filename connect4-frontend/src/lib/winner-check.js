/* All processing is to be done before tranposition */

const winners = [null, 'player', 'agent']
const NUM_ROW = 6; 
const NUM_COL = 7;

const checkHorizontal = (board) => {
    
    const subArrays = []
    for (let i = 0; i < NUM_COL; i++){
        
    }

    return 
}

const checkVertical = (board) => {

}

const checkDiagonal = (board) => {

}



const checkWinner = () => {
    return null
}

export default checkWinner;
